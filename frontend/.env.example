# Backend API URL for local development.
# Empty or /api: use relative path (Vite dev proxy or Docker/nginx reverse proxy).
# http(s)://...: use absolute URL when the API runs on another host.
VITE_API_URL=
