/**
 * Runtime configuration for the Data Labelling App frontend.
 * Loaded before the app so window.APP_CONFIG is available at startup.
 * Override in deployment by replacing this file or serving a custom one (e.g. from env).
 */
window.APP_CONFIG = {
  apiUrl: "/api"
};
